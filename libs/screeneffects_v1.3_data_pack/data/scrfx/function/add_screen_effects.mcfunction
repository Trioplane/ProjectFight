# You're probably wondering why this exists.
# Well because sometimes people like to not follow instructions
# and break other's people packs because they probably used
# 'set' instead of 'merge' so that's why this 1 line of macro
# exists lol.
$data modify storage scrfx:registry screen_effects merge from storage $(storage) $(path)
